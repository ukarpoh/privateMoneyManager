BOT_TOKEN=your_telegram_bot_token_here
KIMI_API_KEY=your_kimi_api_key_here

# Optional: currency symbol shown in all displays (default: RM)
CURRENCY_SYMBOL=RM

# Data paths — default to /data/ (persistent volume in Docker/Railway).
# Override for local dev outside Docker:
# DB_PATH=budget_bot.db
# LOG_PATH=budget_bot.log
